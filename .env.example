# Production site URL (used for canonical links, sitemap, robots.txt, and OG metadata)
NEXT_PUBLIC_SITE_URL="https://lincolnshirekneeclinic.co.uk"

# Business Dashboard Authentication
# The dashboard now uses Supabase Auth. Create an admin user in Supabase Auth,
# then mark that user as an admin in community_profiles using the SQL comment
# block in supabase_community_setup.sql.
# Leave MFA off until the admin account has visited /portal/business/mfa and
# verified an authenticator app, then set this to "true" and redeploy.
REQUIRE_ADMIN_MFA="false"

# Secondary PIN for the /portal/clinician-intake shared-terminal tool (on top
# of the Supabase admin session, which is the real access control).
# Verified server-side only via /api/portal/verify-pin — never shipped to the browser.
CLINICIAN_INTAKE_PIN="change-me"

# Supabase Production Database Credentials (EU Frankfurt Region)
NEXT_PUBLIC_SUPABASE_URL="https://your-project-id.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="your-supabase-service-role-key-here"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-supabase-anon-key-here"

# Microsoft Clarity Web Analytics (Optional)
NEXT_PUBLIC_CLARITY_PROJECT_ID="your-clarity-project-id-here"

# Serper.dev API key — powers the "Suggested Evidence Sources" web-search panel
# in the clinical review dashboard (/portal/business, Clinical Review tab).
# Get a key at https://serper.dev
SERPER_API_KEY="your-serper-api-key-here"
